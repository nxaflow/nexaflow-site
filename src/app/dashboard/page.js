import DashboardApp from "@/sites/dashboard/DashboardApp";

export const metadata = {
  title: "NexaFlow Dashboard",
  description: "Client, task, calendar, and availability command center.",
};

export default function DashboardPage() {
  return <DashboardApp />;
}
